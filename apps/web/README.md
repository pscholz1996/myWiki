# myWiki Web

The Next.js app — chat UI, knowledge-base API routes, agent integration
(Claude Agent SDK), ingestion pipeline, and the SQLite search index. See
the [root README](../../README.md) for the feature overview and
[CONTRIBUTING.md](../../CONTRIBUTING.md) for setup.

```bash
pnpm dev    # http://localhost:3000
pnpm test
```
